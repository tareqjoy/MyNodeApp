{
    "name": "post-service",
    "version": "1.0.0",
    "description": "",
    "main": "dist/server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "tsc -p . && node dist/server.js",
        "setup:db:createIndex": "tsc -p . && node dist/db/DbSetup.js --create-index",
        "setup:db:createShard": "tsc -p . && node dist/db/DbSetup.js --create-shard",
        "build": "tsc -p ."
    },
    "author": "Tareq Joy",
    "license": "ISC",
    "dependencies": {
        "axios": "^1.7.4",
        "express": "^4.19.2",
        "log4js": "^6.9.1",
        "class-validator": "^0.14.1",
        "class-transformer": "^0.5.1",
        "reflect-metadata": "^0.2.2",
        "@tareqjoy/clients": "^1.0.17",
        "@tareqjoy/models": "^1.0.42"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "nodemon": "^3.1.4",
        "typescript": "^5.5.4"
    }
}